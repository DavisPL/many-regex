[project]
name = "many-regex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.8",
    "ruff>=0.15.0",
    "rure>=0.2.2",
]
