[project]
name = "many-regex-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyre2>=0.3.10",
    "regex>=2026.1.15",
    "ruff>=0.15.0",
    "rure>=0.2.2",
]
